//@ts-check

/**
 * Esta función pretende sumar dos numeros.
 * @param {number} n1 Es el primer numero a sumar.
 * @param {number} n2 Es el segundo numero a sumar.
 * @returns {number} Es el resultado de la suma de los parámetros.
 */
function sumar(n1, n2) {
    return n1 + n2;
}