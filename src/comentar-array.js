//@ts-check

/**
 * Este es un arreglo que almacena cualquier tipo de dato.
 * @type {Array} 
 */
const arrayTipoDatos = [18, 25.3, false, {atributo: "valor"}];

/**
 * Este es un arreglo que almacena solo datos de tipo int.
 * @type {Array<Number>} 
 */
 const arrayInt = [18, 23, 24, 32];

 /**
 * Este es un arreglo que almacena solo datos de tipo string e int.
 * @type {Array<Number | string>} 
 */
  const arrayIntString = [18, "Hola", 24, "Mundo"];